# ParallelTurnPvp 待办清单

## A. 自动化回归（已纳入）
- [x] A1. 新增 `tools/regression/Run-DualMachineRegression.ps1`，一键执行构建、双机部署、日志拉取、关键字巡检。
- [ ] A2. 固化回归用例：进房、出牌、喝药、结束回合、回合结算、对局结束。
- [ ] A3. 固化一致性巡检：`StateDivergence`、`Nullable object must have a value`、协议不一致、赛后误开回合。
  补充：巡检已覆盖 `CardConsoleCmdInCombat`（控制台注卡风险信号）。
- [ ] A4. 固化玩法巡检：前线独立格挡、溢出伤害、延迟结算开启/关闭双模式。
- [ ] A5. 输出回归报告（通过/失败、关键日志片段、建议回退提交）。

## B. 分房战斗（后续可选路线）
- [x] B1. 分房会话骨架：`RoomSessionId + Topology` 注入 runtime。
- [x] B2. 分房协议骨架：`RoundState/PlanningFrame/RoundResult` 携带并校验房间上下文。
- [x] B3. 分房入口流（v1 壳）：开启分房时每端只显示本地角色与本地假人目标，远端角色视觉隐藏，动作目标映射为敌方侧提交。
- [ ] B3.1 分房入口流（正式版）：联机后进入真正“各自房间”而非同房战斗壳。
- [x] B4. 提交模型切换（v1 通道）：客户端在锁回合时独立上报本房动作批，主机接收并做一致性校验日志。
- [ ] B4.1 提交模型切换（正式）：结算仅依赖提交通道，不再依赖同房即时动作队列副作用。
  当前进度：客户端提交已从“仅锁回合”扩展为“出牌/喝药/锁回合增量发送”；Host 接收后反写动作日志与意图状态，并新增按玩家 revision 单调校验（拒收旧包/乱序包/同 revision 冲突包）。已补 `客户端锁回合后自动重发`、`Host 短超时等待远端 submission`、`Host 接收提交后广播规划帧（轻量 ACK）`，并新增 `独立 ACK/NACK 消息 + 客户端 NACK 后立即补发`。已新增 `Host 自适应 resolve 等待窗口`（最近网络活动/缺失玩家本地已锁定场景自动延长超时）与 `客户端分段重发节奏 + 重发耗尽告警`，继续压低过早 fallback。`Resolver fallback` 已纳入回归与摘要可观测。
- [ ] B5. Host 权威解算：按双方提交批统一结算并下发回放。
- [ ] B6. 客户端只读回放落地：禁止本地直接执行对方动作。
  当前进度：只读结算已提升为默认路径（`PTPVP_ENABLE_CLIENT_READONLY_RESOLVE` 默认开启，可显式设 `0` 关闭）；客户端在结算点跳过本地 `ResolveLiveRound`，仅等待 Host 权威 `RoundResult`。版本守卫已纳入该模式位（`/Oon|off`），并补充了“客户端等待权威结果”秒级观测与超时告警日志，避免静默卡住。
- [ ] B7. 分房异常处理：超时、断线、重发、重复包、会话错配。

## C. 稳定性收口
- [ ] C1. 关键日志分级：`INFO/WARN/ERROR` 统一规范，减少噪点。
- [x] C2. 关键状态快照落盘：每回合主机落地摘要，便于回滚排查。
  落地位置：`%AppData%\\SlayTheSpire2\\logs\\ptpvp_round_summary.ndjson`（host-only）。
- [ ] C3. 对局结束路径收敛：去除尾部无效回合启动日志。
- [ ] C4. 控制台作弊防呆（待定）：在 `ParallelTurnPvP_Debug` 内识别并提示/拦截 `ConsoleCmdGameAction`，避免误测触发 `StateDivergence`。

## D. 重连恢复（已纳入）
- [ ] D1. 连接中断检测与会话保活：对局进入 `DisconnectedPendingResume` 状态，冻结本地操作。
  当前进度：已接入 `NetClientGameService/NetHostGameService` 断线回调，运行时可进入 `disconnected pending resume` 并冻结本地出牌/喝药/结束回合；下一步补“恢复握手与快照回灌”。
- [ ] D2. 主机侧恢复快照：按 `roomSessionId + roundIndex + revision` 提供最近权威状态与未结算提交。
  当前进度：已新增 `ResumeStateRequest/ResumeState` 协议骨架；客户端在 `DisconnectedPendingResume` 时周期请求，Host 返回 `RoundState + PlanningFrame + RoundResult`（若可用），并在客户端应用后清除冻结状态。下一步补重连握手校验与落地回放收敛。
- [ ] D3. 客户端重连握手：恢复后校验协议/内容/分房拓扑一致，再拉取权威快照并回放补齐。
  当前进度：已接入 DirectConnectIP `RunSessionState.Running` 兼容路径（不再被 `RunInProgress` 立即断开），并在客机执行“运行中重连引导”（`ClientRejoinResponseMessage.serializableRun -> SetUpSavedMultiPlayer -> LoadRun`），随后进入 `DisconnectedPendingResume` 拉取 Host 权威恢复态；下一步补协议/拓扑显式校验与 combatState 回灌收敛。
- [ ] D4. 超时与失败策略：超过阈值自动判负/和局（待定），并记录恢复失败原因。
- [ ] D5. 回归用例：断线 5s/15s/30s、主机断开、客户端断开、重连后继续至结算。

