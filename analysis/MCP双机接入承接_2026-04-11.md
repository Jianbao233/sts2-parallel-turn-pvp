# MCP 双机接入承接

## 当前状态
- 主机 `MCPControl` 已安装到 `J:\Tools\MCPControl`
- 主机依赖已落地：
  - `J:\Tools\MCPControl\deps\node-v22`
  - `J:\Tools\MCPControl\deps\python-3.12.10`
  - `J:\Tools\MCPControl\deps\vs2022-buildtools`
- 主机启动脚本已生成：
  - `J:\Tools\MCPControl\Start-McpControl.ps1`
- 主机 `Codex` 配置已写入：
  - `C:\Users\Administrator\.codex\config.toml`
  - `mcp_servers.primary-desktop = http://127.0.0.1:3232/mcp`
  - `mcp_servers.secondary-desktop = http://DESKTOP-U51KJJ2:3233/mcp`

## 主机已验证
- `mcp-control` 已成功安装到 `J:\Tools\MCPControl\npm-global`
- 本机有有效监听：
  - `0.0.0.0:3232`
- 监听进程：
  - `node.exe` from `J:\Tools\MCPControl\deps\node-v22\node.exe`
- 已确认不是安装失败，当前主机 SSE 服务可启动

## 下一会话边界
- 当前这个会话虽然已经把 MCP server 写进 `Codex` 配置，但通常需要 **重开/恢复一个新会话** 后，新的 MCP server 才会真正变成可调用工具。
- 下一次对话开始时，优先先检查：
  - `codex mcp list`
  - `primary-desktop` / `secondary-desktop` 是否都显示 `enabled`

## 副机还需要做的事
- 副机还没有完成技术栈落地与服务启动
- 最新安装脚本已经同步到共享目录：
  - `\\DESKTOP-U51KJJ2\SlayTheSpire2\tools\desktop_mcp\mcpcontrol`

## 副机执行命令
- 建议在 **管理员 PowerShell** 执行

```powershell
Set-ExecutionPolicy -Scope Process Bypass -Force
powershell -ExecutionPolicy Bypass -File "\\DESKTOP-U51KJJ2\SlayTheSpire2\tools\desktop_mcp\mcpcontrol\Setup-McpControlSecondary.ps1" -Port 3233
```

- 安装完成后启动服务：

```powershell
powershell -ExecutionPolicy Bypass -File "J:\Tools\MCPControl\Start-McpControl.ps1" -Port 3233
```

## 副机验证命令
```powershell
netstat -ano | findstr :3233
Get-ChildItem "J:\Tools\MCPControl\logs"
```

## 常用命令
### 主机启动服务
```powershell
powershell -ExecutionPolicy Bypass -File "J:\Tools\MCPControl\Start-McpControl.ps1" -Port 3232
```

### 主机重写 Codex MCP 配置
```powershell
powershell -ExecutionPolicy Bypass -File "K:\杀戮尖塔mod制作\STS2_mod\PVP_ParallelTurn\tools\desktop_mcp\mcpcontrol\Configure-CodexMcp.ps1"
```

### 查看当前 MCP 配置
```powershell
codex mcp list
```

## 关键文件
- `K:\杀戮尖塔mod制作\STS2_mod\PVP_ParallelTurn\tools\desktop_mcp\mcpcontrol\Setup-McpControl.ps1`
- `K:\杀戮尖塔mod制作\STS2_mod\PVP_ParallelTurn\tools\desktop_mcp\mcpcontrol\Setup-McpControlPrimary.ps1`
- `K:\杀戮尖塔mod制作\STS2_mod\PVP_ParallelTurn\tools\desktop_mcp\mcpcontrol\Setup-McpControlSecondary.ps1`
- `K:\杀戮尖塔mod制作\STS2_mod\PVP_ParallelTurn\tools\desktop_mcp\mcpcontrol\Configure-CodexMcp.ps1`
- `K:\杀戮尖塔mod制作\STS2_mod\PVP_ParallelTurn\analysis\双机桌面MCP接入方案.md`

## 已知注意事项
- `MCPControl` 依赖 `keysender`，因此安装阶段必须有可用的 `VS2022 C++ Build Tools`
- 当前脚本已经改成优先把依赖放到 `J:\Tools\MCPControl`
- `Visual Studio Installer` 自身的少量辅助组件仍可能写入系统默认位置，但主安装根和产物目录已经固定到 `J:`
- 当前主机 `3232` 端口已经在用，重复启动前应先确认是否已有旧实例
